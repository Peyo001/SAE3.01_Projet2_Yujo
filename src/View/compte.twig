{% extends "base_template.html.twig" %}

{% block title %}Profil de {{ utilisateur.pseudo }}{% endblock %}

{% block content %}
    <div class="container">
        <h1>Info du compte de {{ utilisateur.pseudo }}</h1>
        <p><strong>Nom:</strong> {{ utilisateur.nom }}</p>
        <p><strong>Prénom:</strong> {{ utilisateur.prenom }}</p>
        <p><strong>Date de naissance:</strong> {{ utilisateur.dateNaissance }}</p>
        <p><strong>Genre:</strong> {{ utilisateur.genre }}</p>
        <p><strong>Pseudo:</strong> {{ utilisateur.pseudo }}</p>
        <p><strong>Email:</strong> {{ utilisateur.email }}</p>
        <p><strong>Type de compte:</strong> {{ utilisateur.typeCompte }}</p>
        <p><strong>Premium:</strong> {% if utilisateur.estPremium %}Oui{% else %}Non{% endif %}</p>
        <p><strong>Date d'inscription:</strong> {{ utilisateur.dateInscription }}</p>
        <p><strong>yuPoints:</strong> {{ utilisateur.yuPoints }}</p>
    </div>

{% endblock %}