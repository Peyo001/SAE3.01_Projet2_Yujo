<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexion Yujo</title>
    <link rel="stylesheet" href="../../node_modules/bootstrap/dist/css/bootstrap.min.css">

    <style>
        body {
            background: linear-gradient(135deg, #4b2cb7 0%, #ff0080 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .container-form {
            width: 100%;
            max-width: 450px;
        }

        .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
            background: #ffffff;
            overflow: hidden;
        }

        .card-header {
            background: linear-gradient(135deg, #4b2cb7 0%, #ff0080 100%);
            border: none;
            padding: 30px 20px;
            text-align: center;
        }

        .card-header .logo {
            width: 50px;
            height: 50px;
            margin-bottom: 15px;
        }

        .card-header h2 {
            color: #ffffff;
            font-size: 28px;
            font-weight: 700;
            margin: 0;
            letter-spacing: 0.5px;
        }

        .card-body {
            padding: 40px 30px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            font-weight: 600;
            color: #333;
            margin-bottom: 8px;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .form-control {
            border: 2px solid #e9ecef;
            border-radius: 8px;
            padding: 12px 15px;
            font-size: 15px;
            transition: all 0.3s ease;
            background-color: #f8f9fa;
        }

        .form-control:focus {
            border-color: #e53282;
            background-color: #fff;
            box-shadow: 0 0 0 0.2rem rgba(229, 50, 130, 0.15);
            outline: none;
        }

        .btn-login {
            width: 100%;
            padding: 12px;
            background: linear-gradient(135deg, #e53282 0%, #ff0080 100%);
            border: none;
            border-radius: 8px;
            color: #ffffff;
            font-weight: 700;
            font-size: 16px;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .btn-login:hover {
            background: linear-gradient(135deg, #d41470 0%, #e60070 100%);
            box-shadow: 0 5px 20px rgba(229, 50, 130, 0.4);
            transform: translateY(-2px);
            color: #ffffff;
            text-decoration: none;
        }

        .btn-login:active {
            transform: translateY(0);
        }

        .form-footer {
            text-align: center;
            margin-top: 25px;
            padding-top: 20px;
            border-top: 1px solid #e9ecef;
        }

        .form-footer p {
            color: #666;
            font-size: 14px;
            margin: 0;
        }

        .form-footer a {
            color: #e53282;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .form-footer a:hover {
            color: #d41470;
            text-decoration: underline;
        }

        .alert {
            border-radius: 8px;
            border: none;
            margin-bottom: 20px;
        }

        .alert-danger {
            background-color: #f8d7da;
            color: #721c24;
        }

        .form-control::placeholder {
            color: #adb5bd;
            font-style: italic;
        }
    </style>
</head>

<body>

<div class="container-form">
    <div class="card">
        <div class="card-header">
            <img src="../public/assets/icons/logo_yujo.png" alt="Logo Yujo" class="logo">
            <h2>Connexion</h2>
        </div>

        <div class="card-body">
            {% if deja_connecte %}
                <div class="alert alert-info" role="alert">
                    Vous êtes déjà connecté{% if pseudo %} en tant que <strong>{{ pseudo }}</strong>{% endif %}.
                    <a href="index.php?controleur=utilisateur&methode=deconnexion" class="ml-2">Se déconnecter</a>
                </div>
            {% endif %}
            {% if error %}
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    {{ error }}
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
            {% endif %}

            <form action="index.php?controleur=utilisateur&methode=traiterConnexion" method="post">

                <div class="form-group">
                    <label for="email">Adresse Email</label>
                    <input type="email" id="email" name="email" class="form-control" placeholder="exemple@email.com" value="{{ last_email }}" required>
                </div>

                <div class="form-group">
                    <label for="password">Mot de passe</label>
                    <input type="password" id="password" name="password" class="form-control" placeholder="Votre mot de passe" required>
                </div>

                <button type="submit" class="btn-login">Se connecter</button>

            </form>

            <div class="form-footer">
                <p>Pas encore de compte ? 
                    <a href="index.php?controleur=utilisateur&methode=afficherFormulaireInscription">S'inscrire</a>
                </p>
            </div>
        </div>
    </div>
</div>

<script src="../../node_modules/bootstrap/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
